# Runtime dependencies, pinned to the versions this app is built and tested
# against (Python 3.14, WSL). Install:  pip install -r requirements.txt
# Regenerate the pins after any intentional upgrade.
flask==3.1.3
mutagen==1.47.0
numpy==2.4.6
essentia-tensorflow==2.1b6.dev1438

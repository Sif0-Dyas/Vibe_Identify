# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Local secrets / config
.env

# Local data / models (large, machine-specific — see project notes)
*.db
genre_v2.db
essentia_models/
*.pb
*.onnx

# Custom-genre training artifacts. The scripts write these OUTSIDE the repo
# (~/genre_training/_cache/, manifest.json; the .npz under ~/essentia_models),
# but ignore them here as insurance against a stray in-repo run.
_cache/
manifest.json
*.npz

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / temp
*.log
tmp/

# Node (frontend lint tooling only)
node_modules/
